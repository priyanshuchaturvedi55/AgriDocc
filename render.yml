services:
  - type: web
    name: agridoc-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: cd AgriDoc/main_app && gunicorn app:app --bind 0.0.0.0:$PORT
    envVars:
      - key: PYTHONPATH
        value: /opt/render/project/src/AgriDoc/main_app
      - key: PYTHONUNBUFFERED
        value: 1
      - key: PYTHON_VERSION
        value: 3.9.0

